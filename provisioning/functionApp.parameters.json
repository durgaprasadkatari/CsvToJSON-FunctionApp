{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "organisationId": {
      "value": "#{organisationId}"
    },
      "domainId": {
      "value": "#{domainId}"
    },
      "resourceInstanceRole": {
      "value": "#{resourceInstanceRole}"
    },
      "environment": {
      "value": "#{environment}"
    },  
    "InstrumentationKey": {
      "value": "#{Core.ApplicationInsights.Key}"
    }, 
    "storageAccountType": {
      "value": "#{storageAccountType}"
    },
    "runtime": {
      "value": "#{runtime}"
    },
    "resourceInstanceId": {
      "value": "#{resourceInstanceId}"
    }
  }
}
